#!/bin/bash

source .env 

case "$1" in 
	android)
		fyne package --target android
		fyne install --os android
		;;
	ios)
		fyne release --target ios --certificate "${IOSDEVCERT}" --profile "${IOSDEVPROF}"
		fyne install --os ios
		;;
	*)
		echo Only android and ios supported currently
		;;
esac	
